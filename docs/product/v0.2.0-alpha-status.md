# v0.2.0-alpha local hardening status

Implementation candidate includes:

- SQLite primary persistence;
- v0.1 local-data import;
- UUIDs, timestamps and versions;
- crew/member/expense edit flows;
- guarded member removal;
- expense audit/detail view;
- paid/share/net balance explanation;
- regression tests and Android CI build.

The next milestone after validation is M5 receipt evidence and QR repayment.
