name: splitcrew_settlement_engine
description: Deterministic balance and settlement engine for SplitCrew.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  splitcrew_domain:
    path: ../domain

dev_dependencies:
  test: ^1.25.8
  lints: ^5.0.0
