name: splitcrew_update_core
description: Distribution-agnostic version and update decision logic for SplitCrew.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  pub_semver: ^2.1.5

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
