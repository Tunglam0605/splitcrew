name: splitcrew_payment_qr
description: Standards-oriented repayment QR payload adapters for SplitCrew.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  splitcrew_domain:
    path: ../domain
  vietqr_core: 0.2.5+1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
