name: splitcrew_local_host
description: Local LAN host transport and invite/session authority for SplitCrew.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  splitcrew_sync_protocol:
    path: ../sync_protocol
  uuid: ^4.5.1

dev_dependencies:
  http: ^1.5.0
  lints: ^5.0.0
  test: ^1.25.8
