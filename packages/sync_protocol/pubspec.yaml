name: splitcrew_sync_protocol
description: Versioned host-authoritative local sync protocol for SplitCrew.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
